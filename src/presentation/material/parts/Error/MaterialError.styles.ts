import styled from "styled-components";
import SectionError from "../../../error/Section/SectionError";

export const Container = styled(SectionError)`
  height: calc(100vh - 120px);
  max-width: 1200px;
  margin: 40px auto 80px;
`;
